services:
  # Web Service
  - type: web
    name: ai-governance-course
    env: node
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: NODE_ENV
        value: production
    plan: free
    region: oregon
    branch: master
